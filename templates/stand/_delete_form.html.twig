<form method="post" action="{{ path('stand_delete', {'id': stand.id}) }}" onsubmit="return confirm('êtes-vous sûr de vouloir supprimer ce stand ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stand.id) }}">
    <button class="btn mt-4 ml-5">Supprimer</button>
</form>
